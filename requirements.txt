# ============================================================
#  SwarmAttack Framework — المتطلبات (متعدد المنصات)
# ------------------------------------------------------------
#  Linux : pip install -r requirements.txt
#  Termux: ثبّت الحزم الجاهزة أولاً (install.sh يفعلها تلقائياً):
#          pkg install python-cryptography python-cffi python-pyyaml
#                   python-rich python-aiohttp
#          ثم: pip install --no-build-isolation -r requirements.txt
#
#  تحذير: لا تحاول بناء cryptography من المصدر على Termux —
#         يتطلب maturin وهدف rustup لأندرويد غير مدعوم.
#         استخدم pkg install python-cryptography دائماً.
# ============================================================

# قلب الأداة — اتصالات TLS ومحاكاة بصمة المتصفح (له wheel جاهز لأندرويد)
curl_cffi>=0.7.2

# نقل غير متزامن (يُثبَّت عبر pkg على Termux)
aiohttp>=3.9.5

# التشفير AESGCM/ChaCha20Poly1305 (عبر pkg على Termux، وpip على Linux)
cryptography>=42.0.8

# قاعدة بيانات غير متزامنة (بايثون خالص — يعمل في كل مكان)
aiosqlite>=0.20.0

# قراءة config/attack_policies.yaml
PyYAML>=6.0.1

# واجهة الطرفية (جداول/تقدم)
rich>=13.7.1

# ===== اختيارية — علّق عليها إن لم تكن بحاجتها =====
# scapy>=2.5.0          # --capture-profile (Linux: pip install scapy / Termux: pkg install python-scapy + root)
# playwright>=1.44.0    # فحص DOM-XSS (لا يعمل على Termux — الكود يتجاهله تلقائياً)
# aiohttp-socks>=0.8.4  # دعم SOCKS5 في proxy_mesh
